---
description: Relapse shop 3D gun preview — T1 etalon (5 guns), assemble per gun
globs: gamemodes/zombiesurvival/gamemode/vgui/relapse_ui.lua,gamemodes/zombiesurvival/gamemode/vgui/dmodelpanelex.lua,gamemodes/zombiesurvival/gamemode/sh_usefulness_model.lua,addons/zzz_relapse_mw_sykov/**/*.lua
alwaysApply: false
---

# Relapse 3D-превью в карточке шопа

## Визуал бокса — эталон T1 (6 стволов)

Один ствол, профиль сбоку, орбита вокруг видимого центра, воздух вокруг, не впритык к краям. Высота в рамке одна линия у всех шести. Код: `RelapseUI.PreviewEtalon` + `SetShopPreview` → `DrawShopPreviewGun`. Иконки — отдельное правило.

Эталон кадра и позиционки: **Макаров, 1911, .357, 680, СКС, Uzi**. Не ломай подписанные числа. Новый ствол собирай по разделу «Сборка», кадр подгоняй к ближайшему по длине из таблицы — не копируй Lift/CamScale СКС на короткий ствол.

### Кадр (общий)

```lua
-- RelapseUI.PreviewEtalon
FOV = 43
CamScale = 1.4                -- дефолт нового ствола; пистолеты см. таблицу
Lift = 3.5                    -- дефолт нового ствола; не для пистолетов
SpanMul = 1.32
CamDir = Vector(0.82, 0.52, 0.34)
dist = clamp(span * SpanMul * CamScale, 20, 96)
LookAt = Vector(0, 0, -Lift)
CamPos = dist * CamDir + LookAt
orbit yaw = RealTime() * 28
```

Свет тот же у всех (`FrameModelPanel`). Новый ствол без Lift/CamScale берёт CamScale 1.4 и Lift 3.5, потом подгоняется к линии таблицы.

Offset крутится с моделью. «Выше/ниже в рамке» = `PreviewLift`. «Ближе/дальше» = `PreviewCamScale`. Pitch 8 + камера сверху = дуло вниз: у ствола вдоль −X `Angle(0, 90, 0)`, не `Angle(8, 90, 0)`.

### Позиционка (зафиксировано)

Не уравнивай Lift/CamScale у всех. Короткий ствол ближе к камере: тот же Lift 3.5 задирает пистолеты.

| Ствол | Angle | LocalAng | Offset | Lift | CamScale |
|---|---|---|---|---|---|
| Макаров | `(0, 90, 0)` | — | — | **0.5** | **1.65** |
| 1911 | `(0, 0, 0)` | `(0, 0, 90)` | `(-0.8, 0, 0.5)` | **0.7** | 1.4 |
| M19 | `(0, 0, 0)` | `(0, 0, 90)` | `(-0.8, 0, 0.5)` | **0.7** | 1.4 |
| .357 | `(0, 0, 0)` | `(0, 0, 90)` | — | **0.7** | **1.65** |
| 680 | `(0, 90, 0)` | — | `(0, 0, -1.2)` | **2.2** | 1.4 |
| СКС | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -5)` | **3.5** | 1.4 |
| Uzi | `(0, 0, 0)` | `(0, 0, 90)` | — | **2.2** | 1.4 |
| MP5 | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -2)` | **2.2** | 1.4 |
| UMP-45 | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -2)` | **2.2** | **1.6** |
| SCAR-H | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -3)` | **2.8** | 1.4 |
| AS VAL | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -3)` | **2.8** | 1.4 |
| АК-47 | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -3)` | **2.8** | 1.4 |
| M4A1 | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -3)` | **2.8** | 1.4 |
| Origin-12 | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -3)` | **2.8** | 1.4 |
| JAK-12 | `(0, 0, 0)` | `(0, 0, 90)` | `(0, 0, -3)` | **2.8** | **1.0** |
| ПКМ | `(0, 90, 0)` | — | `(-6, 0, -3)` | **2.8** | 1.4 |
| FiNN LMG | `(0, 90, 0)` | — | `(-6, 0, -3)` | **2.8** | 1.4 |

Ближайший эталон для нового ствола: короткий −X → Макаров; короткий +Z → 1911/.357/M19; длинный −X / **+X** → 680 yaw 90 (ПКМ/FiNN: hull вдоль +X); длинный +Z → СКС; средний +Z (ПП) → Uzi; автомат +Z (SCAR, VAL, АК, Origin-12, M4A1, JAK-12) → сборка СКС (кадр SCAR Lift 2.8 Offset −3). У M4 default receiver — отдельный mdl, не пустой `att_receiver` АК. У JAK-12 default stock/grip — `att_stock` / `att_grip`, не копировать Origin sidegrip/stock. Не копировать MP5 `[0]=1` на АК/SCAR/ПКМ/FiNN/M4/JAK-12. Не копировать 680 yaw 90 на Origin-12/JAK-12 (у 680 пустой WM, у Origin/JAK ресивер на variant 0). Не копировать SCAR `LocalAng 90` на ПКМ/FiNN (у SCAR hull +Z, у ПКМ/FiNN +X: roll крутит вокруг ствола).

## Сборка — не копируй соседа

Сначала выясни, **какие mdl — сам ствол**, и в **каком пространстве** их origin. У пяти эталонов один пайплайн, разные кости и разный набор мешей на WM.

Пустой 3D-бокс — это не «камера далеко». `DrawShopPreviewGun` рисует только то, у чего `DrawModel` на **дефолтном** bodygroup что-то показывает. Нет меша → пустая карточка.

### Перед сборкой

Открой `customization.lua` (слоты по умолчанию — первые в каждом ряду) и mdl:

1. Что **реально рисует** `w_*.mdl` при identity + bodygroup 0 (`DrawModel`, не сумма VVD). Это не аттач.
2. Какие дефолтные аттачи реально рисуют пушку (barrel, slide, mag, grip, receiver, pump). Первый ключ в ряду — дефолт. `att_stock` / `att_magazine` без своей mdl — кусок уже на receiver/WM, не выдумывай.
3. У аттача `ShowOnWorldModel = false`? Кость `GetBoneName(0)` есть **на WM**? Нет → в bone merge не класть (680: receiver `j_gun`, на `w_romeo870` её нет; SKS: `j_gun` есть, parent −1 — тоже не dummy).
4. Вершины: VVD AABB. Один origin (Макаров: всё вдоль −X) или разные кости (WM + аттачи).

VVD vert count **не** значит «WM уже пушка». У 680 `w_romeo870` ~30k вершин (все bodygroup в файле) — риг без ствола. Считать пушку на WM можно только если дефолтный `DrawModel` её показывает (.357: цилиндр). Иначе пушка в аттачах.

### Что рисовать

MW `v_` / `w_` часто пустые: руки + stub. Пушка сидит в аттачах **или** на WM-рамке. Не клади в `PreviewParts` один `w_*.mdl`, пока не проверил пункт 1.

| Сборка | Когда | Как |
|---|---|---|
| Один origin | Аттачи делят пространство (Макаров: barrel + grip вдоль −X; 680: receiver + barrel + pump у `j_gun`, сдвиг ~1 hu) | `RelapsePreviewParts` строками, **без** bone merge. Dummy = `[1]`. |
| Bone merge | Рамка на WM, куски на других костях (1911: slide/mag; СКС: barrel/mag/stock) | `[1]` = `w_*.mdl` (не `v_`), `RelapsePreviewBoneMerge = true`, остальные EF_BONEMERGE на dummy. У **каждого** экстра `GetBoneName(0)` есть на WM. |
| Кусок уже на WM | .357: цилиндр/grip на `w_357`; mag `ShowOnWorldModel=false` | Не дублировать. Лишний mdl без кости WM зависает в origin и уводит пивот. |
| WM — пустой риг | 680: `w_romeo870` не рисует ствол, `j_gun` нет | Не dummy-only WM. Не bone-merge receiver на эту WM. Дефолтные аттачи, один origin. |
| WM mesh не на bodygroup 0 | Uzi: `w_uzulu` receiver `[0]` = 0 verts, `[1]` = вся пушка | Dummy = WM + bodygroup 1. DrawModel после SetupBones вдоль +Z — hull + LocalAng 90 как СКС. Yaw 90 ставит ПП вертикально. |
| WM mesh на bodygroup 0, `[1]` пустой | АК / SCAR / Origin-12 / JAK-12 / ПКМ / FiNN: `w_akilo47` / `w_scharlie` / `w_oscar12` / `w_aalpha12` / `w_pkilo` / `w_sierrax` receiver `[0]` = коробка, `[1]` = 0 verts | **Не** копировать MP5 `[0]=1` — спрячет коробку, приклад останется на `tag_stock_attach` с дырой. Дефолт 0, как СКС. Origin-12 / JAK-12: не копировать 680 (пустой WM). ПКМ/FiNN: сборка как СКС, углы как 680 (hull +X). |
| WM — только рамка, пушка в аттачах | MP5: `w_mpapa5` `[0]=1` = receiver, mag/stock/barrel на `tag_*_attach` | Как СКС: dummy WM + extras. Не копируй Uzi (у Uzi вся пушка уже в variant 1). Кадр как Uzi: Lift 2.2, CamScale 1.4. |
| Нет отдельного grip-аттача | Рамка = bodygroup receiver на `w_` **или** на att receiver (680: приклад на receiver) | Не выдумывай grip.mdl / stock.mdl. |

```lua
-- ❌ вьюмодель в панели (idle/руки = второй ствол)
RelapsePreviewParts = { "models/viper/mw/weapons/v_m1911.mdl", slide, mag }

-- ❌ WM как единственная часть, потому что «в VVD много вершин» (680: пустой бокс)
RelapsePreviewParts = { "models/viper/mw/weapons/w_romeo870.mdl" }

-- ❌ все mdl в одном origin, если кости разные (слипшиеся/два ствола)
-- ❌ bone-merge VM-магазина без костей на WM (летающий спидлоадер)
-- ❌ bone-merge receiver `j_gun` на WM без `j_gun` (рамка в origin)
-- ❌ Uzi как СКС: default WM пустой, extras без receiver → обломки в углу hull
-- ❌ Uzi yaw 90: DrawModel вдоль +Z, ствол стоит вертикально
-- ❌ MP5 как Uzi: только WM — рамка без магазина и приклада
-- ❌ АК/SCAR/Origin-12/ПКМ/FiNN/M4/JAK-12 как MP5: `[0]=1` прячет коробку (у них variant 1 пустой / его нет)
-- ❌ Origin-12/JAK-12 как 680: yaw 90, без bone merge — WM уже ресивер вдоль +Z

-- ✅ Макаров — эталон короткого −X; WM пустая, barrel + grip
RelapsePreviewParts = { barrel, grip }
RelapsePreviewAngle = Angle(0, 90, 0)
RelapsePreviewLift = 0.5
RelapsePreviewCamScale = 1.65

-- ✅ 1911 — эталон короткого +Z; рамка WM, затвор и магазин на костях
RelapsePreviewBoneMerge = true
RelapsePreviewParts = { w_m1911, slide, mag }
RelapsePreviewAngle = Angle(0, 0, 0)
RelapsePreviewLocalAng = Angle(0, 0, 90)
RelapsePreviewOffset = Vector(-0.8, 0, 0.5)
RelapsePreviewLift = 0.7
RelapsePreviewCamScale = 1.4

-- ✅ M19 — короткий +Z как 1911; рамка w_p320, slide/mag без подпапки
RelapsePreviewBoneMerge = true
RelapsePreviewParts = { w_p320, papa320_slide, papa320_mag }
RelapsePreviewAngle = Angle(0, 0, 0)
RelapsePreviewLocalAng = Angle(0, 0, 90)
RelapsePreviewOffset = Vector(-0.8, 0, 0.5)
RelapsePreviewLift = 0.7
RelapsePreviewCamScale = 1.4

-- ✅ .357 — эталон короткого +Z; цилиндр уже на w_357, только рамка + ствол
RelapsePreviewBoneMerge = true
RelapsePreviewParts = { w_357, barrel }
RelapsePreviewAngle = Angle(0, 0, 0)
RelapsePreviewLocalAng = Angle(0, 0, 90)
RelapsePreviewLift = 0.7
RelapsePreviewCamScale = 1.65

-- ✅ 680 — эталон длинного −X; WM пустая, receiver + barrel + pump
RelapsePreviewParts = { receiver, barrel, pump }
RelapsePreviewAngle = Angle(0, 90, 0)
RelapsePreviewOffset = Vector(0, 0, -1.2)
RelapsePreviewLift = 2.2
RelapsePreviewCamScale = 1.4

-- ✅ СКС — эталон длинного +Z; hull после SetupBones, не файл приклада
RelapsePreviewBoneMerge = true
RelapsePreviewHullBounds = true
RelapsePreviewParts = { w_sksierra, barrel, mag, stock }
RelapsePreviewAngle = Angle(0, 0, 0)
RelapsePreviewLocalAng = Angle(0, 0, 90)
RelapsePreviewOffset = Vector(0, 0, -5) -- к прикладу; hull начинается у руки
RelapsePreviewLift = 3.5
RelapsePreviewCamScale = 1.4

-- ✅ Uzi — средний +Z; default bodygroup пустой, DrawModel вдоль +Z
RelapsePreviewBoneMerge = true
RelapsePreviewHullBounds = true
RelapsePreviewBodygroups = { [0] = 1 }
RelapsePreviewParts = { w_uzulu }
RelapsePreviewAngle = Angle(0, 0, 0)
RelapsePreviewLocalAng = Angle(0, 0, 90)
RelapsePreviewOffset = Vector(0, 0, 0)
RelapsePreviewLift = 2.2
RelapsePreviewCamScale = 1.4

-- ✅ MP5 — средний +Z как Uzi, сборка как СКС (рамка + mag/stock/barrel)
RelapsePreviewBoneMerge = true
RelapsePreviewHullBounds = true
RelapsePreviewBodygroups = { [0] = 1 }
RelapsePreviewParts = { w_mpapa5, barrel, mag, stock }
RelapsePreviewAngle = Angle(0, 0, 0)
RelapsePreviewLocalAng = Angle(0, 0, 90)
RelapsePreviewOffset = Vector(0, 0, -2) -- hull = receiver; приклад сзади
RelapsePreviewLift = 2.2
RelapsePreviewCamScale = 1.4

-- ✅ SCAR-H / AS VAL / АК-47 / Origin-12 / M4A1 / JAK-12 — длинный +Z, сборка как СКС (не MP5 bodygroup).
-- Путь аттача как в lua пака (SCAR без подпапки, VAL: attachments/valpha/, АК: attachments/akilo47/,
-- Origin-12: attachments/oscar12/, M4: attachments/mike4/, JAK-12: attachments/aalpha12/).
-- Receiver уже на WM variant 0 у АК/SCAR/Origin/JAK; у M4 default receiver — отдельный mdl, класть в PreviewParts.
-- `[0]=1` у АК/SCAR/Origin-12/M4/JAK пустой. Origin-12 default grip slot is a real sidegrip, not att_grip — include it.
-- JAK-12 / M4 default grip is att_grip. JAK default stock is att_stock — do not invent Origin stock.
-- Do not copy 680.
RelapsePreviewBoneMerge = true
RelapsePreviewHullBounds = true
RelapsePreviewParts = { w_scharlie, barrel, mag, stock } -- VAL: w_valpha + valpha/...; AK: w_akilo47 + akilo47/...; Origin-12: w_oscar12 + oscar12/... + sidegrip; M4: w_mike4 + mike4 receiver/barrel/mag/stock; JAK-12: w_aalpha12 + aalpha12 barrel/mag
RelapsePreviewAngle = Angle(0, 0, 0)
RelapsePreviewLocalAng = Angle(0, 0, 90)
RelapsePreviewOffset = Vector(0, 0, -3)
RelapsePreviewLift = 2.8
RelapsePreviewCamScale = 1.4

-- ✅ ПКМ / FiNN LMG — сборка как СКС (WM + barrel/mag/stock), углы как 680.
-- Studio hull вдоль +X (дуло +X), не +Z. LocalAng 90 = roll вокруг ствола, пушка на боку.
-- Default grip is att_grip — do not invent a bipod. Do not copy SCAR LocalAng 90.
-- ПКМ: attachments/pkilo/, w_pkilo. FiNN: attachments/sierrax/, w_sierrax.
RelapsePreviewBoneMerge = true
RelapsePreviewHullBounds = true
RelapsePreviewParts = { w_pkilo, barrel, mag, stock } -- FiNN: w_sierrax + sierrax/...
RelapsePreviewAngle = Angle(0, 90, 0)
RelapsePreviewLocalAng = Angle(0, 0, 0)
RelapsePreviewOffset = Vector(-6, 0, -3) -- hull center is muzzle-heavy; -X toward the stock
RelapsePreviewLift = 2.8
RelapsePreviewCamScale = 1.4
```

Dummy = `parts[1]`. Без bone merge панель **не** рисует dummy, только extras (с индекса 1). Один `w_*` без меша на DrawModel → пустой бокс. Для shop `DModelPanelEx` **не** вызывает engine `DrawModel` (idle). Не играть `idle`/`inspect` на MW VM.

## Углы

`RelapsePreviewAngle` — орбита. Длинный/короткий ствол вдоль −X **или +X**: yaw 90, **pitch 0** (Макаров, 680, ПКМ, FiNN). Ствол вдоль +Z: `Angle(0,0,0)` + `LocalAng(0,0,90)` (1911, M19, .357, СКС, Uzi, SCAR, M4, JAK-12). Yaw 90 на Uzi/SCAR ставит ствол вертикально. Yaw 90 на ПКМ/FiNN — профиль: hull вдоль +X.

`RelapsePreviewLift` / `CamScale` — только из таблицы эталона. Не копировать СКС 3.5 / 1.4 на пистолет.

Ствол **не** вдоль −X (WM в руке, при identity ствол вверх / +Z): **не** копируй `Angle(8, 90, 0)` и не yaw 90. Yaw 90 покажет торец. Орбита `Angle(0,0,0)` + local roll 90. Исключение: ПКМ/FiNN identity уже +X — yaw 90 как 680, **без** LocalAng 90.

Не складывай в один Euler `Angle(8, 90, 90)`. В `OrbitShopPreview`: матрица `orbit * local`, пивот той же матрицей. Не `Vector:Rotate` при ненулевом roll.

Ось смотри по **DrawModel** после SetupBones (hull / posed), не по одному VVD. У СКС bind pose вдоль +X, в руке ствол вверх / +Z — yaw 90 ставит винтовку вертикально.

## Центр орбиты

Не объединять локальные AABB аттачей до parenting.

Bone merge, `assembledPreviewBounds`: identity + `SetupBones`.

| Что | AABB |
|---|---|
| Рамка dummy (`w_*`) | Обычно `GetModelMeshes` **без** сдвига (1911 bind pose = DrawModel). Если DrawModel вдоль +Z, а VVD вдоль +X (СКС): `RelapsePreviewHullBounds` → `GetRenderBounds` после SetupBones, extras в AABB не мешать. |
| Аттачи | `GetBoneName(0)` аттача, матрица **этой** кости на родителе. Не при HullBounds. |

Не множить рамку на `tag_pistol_offset`. У 1911 кость в origin — сдвиг случайно нулевой. У .357 bind pos ≈ `(−1, +2, +1)` — пивот уезжает, ствол **проваливается** ниже 1911.

Камера смотрит в origin; dummy `SetPos` так, чтобы посчитанный центр там оставался. Лишний mdl в origin раздувает span → камера уезжает, пушка мелкая и в углу.

`RelapsePreviewOffset` — сдвиг **пивота** в пространстве модели. У 680 приклад тянет AABB вниз, `Vector(0, 0, -1.2)`. У 1911 AABB в пустом углу под стволом, пивот к колодцу: `Vector(-0.8, 0, 0.5)`. У СКС hull от руки, пивот к прикладу: `Vector(0, 0, -5)`. У Uzi offset нулевой: крутить центр `GetRenderBounds` после bodygroup 1. У MP5 hull = receiver, приклад сзади: `Vector(0, 0, -2)`. Не класть hull приклада СКС в AABB — файл аттача раздувает span.

## Куда писать

Одинаковые `PreviewParts` / `PreviewBoneMerge` / `PreviewHullBounds` / `PreviewBodygroups` / `PreviewAngle` / `PreviewLocalAng` / `PreviewOffset` / `PreviewLift` / `PreviewCamScale` в SWEP, `GM.RelapseWeapons`, `BindRelapseWeapon`, `ApplyOne` (`relapse_mw_sykov_zs.lua`).

Проверка: `changelevel` (не `restart`). Пустой бокс = части не рисуются, не крути FOV.
