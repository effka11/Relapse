---
description: Relapse ladder hold — FLY unsticks hatches; NOCLIP on hold. Closed ceiling is a horizontal brush line from air, not FLY.
globs: gamemodes/zombiesurvival/gamemode/player_movement/**/*ladder*,gamemodes/zombiesurvival/gamemode/player_movement/shared/player_movement.lua,gamemodes/zombiesurvival/entities/entities/relapse_ladder_clip.lua
alwaysApply: true
---

# Relapse лестница: hold сквозь люк

Кастомный клип, не `MOVETYPE_LADDER`.

**Чуть в стене, чем глубже тем сильнее оттяг, спринт чуть дальше** — engine unstick. `Move` true не отменяет коллизию мира у `FLY`. Не hop / duck hull / extra 8 / `FloorUnder` 12×12 / snap `SeatXY(0)` / `PullHitPos`.

**Сквозь глухой потолок** — hold `NOCLIP` без стопа по Z. Не возвращать `FLY`.

Код: `SetClimbing` / `ClimbWorldStop` / `RelapseLadderClimb` в `relapse_ladder.lua`. `GM:Move` зовёт climb **до** early-out по noclip. Наш hold-noclip не `RelapseNoclip`: climb не `return false` из-за `GetMoveType()==NOCLIP`.

| | |
|---|---|
| Hold | `MOVETYPE_NOCLIP` |
| XY | `SideDir`: сторона, с которой нажали E. `Outward` в стену не сажает. Точка в мире не оставлять |
| Потолок | BRUSHONLY из воздуха: вверх голова `hull.z`, вниз ноги. Hit, не небо, abs n.z > 0.5, и полка продолжается на 28 наружу. Узкий выступ стены — не стоп. Вверх: полка ниже верха колонки на 16 — лестница сквозь неё. Вниз: полка выше низа колонки на 16 пропускается, пол у низа останавливает и отпускает |
| Люк | линия в дырку **или** `StartSolid` в плите — dest не трогать |
| Filter | все `RelapseLadderClip` |
| Leave | край `ClimbZRange` и stand hull не `StartSolid` |
| Space | прыжок к взгляду, `JumpOff` / `UnstickForLeave` |
| E | шаг на пол рядом (`FindPlatformStep`), коридор корпуса чист. Нет пола — удержание не снимать |
| Ступени | `WeldColumns`: одна грань, сдвиг по XY до 96 и зазор по Z до 192. Уступ глубже 36 всё ещё стена (луч 100). Дыра без стены на этих 100 не склеивается |
| Выше объёма | `prop_static` с `ladder` в модели читается из BSP: это не сущность, `ents.FindInBox` её не видит. Сегменты одной колонки (XY 48, зазор по Z до 32) поднимают объём до верха модели. Красный ящик в виде меша тянется до того же верха; площадки ботов не двигаются |
| Не трогать | `OpenFaceSlabs`, `STAND_OFF` 24, `IN_DUCK`, `ClimbZRange` |
| Ghost | пропы. `RelapseNoclip` не перехватывать |

Extra 16 над глухим полом линия остановит — это не дырка. Проход, если XY посадки в отверстии.

```lua
-- ❌ FLY (unstick) / стоп StartSolid (люк) / HitPos по XY
pl:SetMoveType(MOVETYPE_FLY)
if tr.StartSolid or math.abs(n.z) <= 0.5 then dest = PullHitPos(tr) end

-- ✅ NOCLIP; стоп только горизонталь из воздуха
pl:SetMoveType(MOVETYPE_NOCLIP)
dest = Vector(origin.x, origin.y, destZ)
if not tr.StartSolid and tr.Hit and math.abs(tr.HitNormal.z) > 0.5 then
	dest = Vector(dest.x, dest.y, origin.z)
end
```

`changelevel`, не `restart`.
